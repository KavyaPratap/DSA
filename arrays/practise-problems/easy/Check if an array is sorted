/*core logic
Core logic
Compare adjacent elements.
If I ever find:
arr[i] > arr[i+1] → not sorted (ascending)
arr[i] < arr[i+1] → not sorted (descending)
If the loop finishes → sorted.
*/

#include <iostream>
using namespace std;
int main(){
    int arr[10]={6,10,23,42,145,82,41,98,76,56};
    int n = 10;
    bool asc = true;
    for(int i = 0; i < n - 1; i++){
        if(arr[i] > arr[i+1]) asc = false;
    }
    if(!asc)
        cout << "Array is not sorted";
    else
        cout << "Array is sorted";
}
